checkout:
  pre:
    - export GOPATH=$GOPATH:`pwd`
  post:
    - ls
    - pwd
    
dependencies:
  post:
      - go get github.com/golang/lint/golint
      
test:
  pre:
    - go version
    - ls
    
  override:
    - cd server;make


